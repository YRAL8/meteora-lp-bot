# Версии прибиты намеренно. Правила обновления — DEPENDENCIES.md.
#
# Без точных версий каждая пересборка образа тянула с PyPI то, что лежит
# свежайшего в эту минуту: попадёшь в окно, пока отравленный релиз ещё не
# сняли, — получишь его на машину, где лежит ключ от кошелька.
#
# Слепок снят 2026-08-12 с работающего боевого контейнера; локальное
# окружение разработки на тот момент совпадало с ним до версии.

# --- прямые зависимости ---
python-dotenv==1.2.2
requests==2.34.2
python-telegram-bot==22.8

# --- транзитивные (тянутся тремя строками выше; прибиты, чтобы pip не
#     разрешал их заново при каждой сборке) ---
anyio==4.14.2
certifi==2026.7.22
charset-normalizer==3.4.9
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.18
typing_extensions==4.16.0
urllib3==2.7.0
